from main import MasterAgent, WorkerAgent

master_agent = MasterAgent()
master_agent.run("Who nominated the only Featured Article on English Wikipedia about a dinosaur that was promoted in November 2016?")


# worker_agent = WorkerAgent("Worker", "")
# worker_agent.run("Gather info about Lupus")

